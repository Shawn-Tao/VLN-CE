<!--
 * @Author: gyx199732 87112540@qq.com
 * @Date: 2025-01-06 17:05:14
 * @LastEditors: gyx199732 87112540@qq.com
 * @LastEditTime: 2025-01-11 21:50:02
 * @FilePath: /TZL/VLN/调试记录.md
 * @Description: 这是默认设置,请设置`customMade`, 打开koroFileHeader查看配置 进行设置: https://github.com/OBKoro1/koro1FileHeader/wiki/%E9%85%8D%E7%BD%AE
-->

# 操作步骤、环境与脚本记录

## 1. 使用habitat-lab生成数据集：

`conda` 环境为服务器本机的 `habitat-latest`，代码文件为 `/home/sy1106/HDD/TZL/VLN/latest_version/my_habitat-sample/gt_follower_example.py`

```python
conda activate habitat-latest
cd /home/sy1106/HDD/TZL/VLN/latest_version/my_habitat-sample/
python gt_follower_example.py
```

记得配置安装基础组件

```zsh
pip install \
  "pip<24.1" \
  "setuptools==65.5.1" \
  "wheel==0.38.4" \
  "packaging==23.2"
```

## 2. 基于上述数据集，生成用于 `llama-facory` 训练的 `json` 文件:

`conda` 环境为服务器本机的 `habitat-latest`，代码文件为 `/home/sy1106/HDD/TZL/VLN/latest_version/my_habitat-sample/utils/llama-fac-json-create.py`

```python
conda activate habitat-latest
cd /home/sy1106/HDD/TZL/VLN/latest_version/my_habitat-sample/
python utils/llama-fac-json-create.py
```

## 3. 使用 `llamafactory` 进行训练

## install

目前安装了两个版本:

### 最新版本

```
/LLM-VLM/python311_workspace/LlamaFactory
```
中为最新版本，对应的conda环境为：`llama-factory-py11`, 升级到了python3.11以应对一些新语法。但是因为unsloth目前没有跟上版本，所以没有配置。
这个版本主要是为了应对一些新模型在transformers 5.0 版本才被引入。

### 稳定版本 - 0.9.4

```
/LLM-VLM/LLaMA-Factory 
```
中为老版本，对应conda环境为`base`, python版本为3.10，配置了对应的unsloth


### wandb 项目命名：

```zsh
export WANDB_PROJECT=llamafactory_orpo
```

### 参数说明书：

[llama-factory参数说明](https://llamafactory.readthedocs.io/zh-cn/latest/advanced/arguments.html)

需要在 `docker` `nvir2405-cu124` 中进行

```bash
docker exec -it 78c7233045d2c6d52413ed95697b7e92ac81318a1f4395b8d3fdd1ff1ab670d zsh
cd /LLM-VLM/LLaMA-Factory
python cli_test.py train my_config/qwen2vl_lora_sft.yaml
```


## 4. `vlnce` `r2r` 数据生成与 `rxr` 数据生成

需要在本机 `vlnce` conda 环境下

__r2r数据生成:__

```shell
python my_run.py --exp-config vlnce_baselines/config/r2r_baselines/test_set_inference.yaml --run-type gt_record_r2r
```

参数解析：config文件为用于生成r2r数据集的配置文件，run-type为程序运行选项，根据r2r数据集有一些内容定制

__rxr数据生成:__

```shell
python my_run.py --exp-config vlnce_baselines/config/rxr_baselines/rxr_cma_en_tzl.yaml --run-type gt_record_rxr
```
参数解析：config文件为用于生成rxr数据集的配置文件，run-type为程序运行选项，根据rxr数据集有一些内容定制


# 关于图像分辨率的一些讨论

Qwen2.5-VL中，图像所占的token数由以下公式进行计算：

$$
Token数量 = ([\frac{宽度}{28}] \times [\frac{高度}{28}])
$$

所以，历史图像处理到 `112 x 56` 分辨率，单张图像 8 个token，当前图像处理到 `424 x 240` 分辨率（与realsense输出图像分辨率相同）,单张图像大约 144 个token

图像缩放方法参考

![alt text](images/image-2.png)

# llamafactory 中的 PPO 算法介绍--包含代码实现与算法介绍


# 强化学习特权信息记录 -- habitat坐标系定义

habitat中，遵从y轴向上，z轴向前，x轴朝右的右手系

在生成的topdown图中，相关定义如下，y轴向上的，所有右转为逆，左转为正
```
旋转初始位置---对应四元数(1,0,0,0)
   /\
   ||
   ||
   ||
    --------->  x轴朝向
   ||
   ||
   ||
   \/
z轴朝向
```
**在r2r数据中，记录的格式如下：**

```
start_position: x,y,z
start_rotation: x,y,z,w
```
**在仿真运行过程中，得到的agent姿态信息，四元数表示为：w，x，y，z**



# 5. 仿真环境基于大模型推理 r2r

## 5.1 基于vlnce的仿真环境

需要在本机 `vlnce` conda环境下，该脚本会将仿真环境中收集到的数据通过tcp发送给LLM运行程序，并接收LLM推理得到的结果，进行运动控制。

```shell
python my_run.py --exp-config vlnce_baselines/config/r2r_baselines/test_set_inference.yaml --run-type qwen
```
## 5.2 基于llama-factory中vllm脚本的大模型推理

需要在 docker 环境下

```shell
docker exec -it 78c7233045d2c6d52413ed95697b7e92ac81318a1f4395b8d3fdd1ff1ab670d zsh
python scripts/vllm_infer_deploy.py --model_name_or_path /LLM-VLM/train_saves/qwen2_vl-7b-R2R-320-5-20epoch-20250612/output --template qwen2_vl
```

注意，脚本运行时输入的参数 `model_name_or_path` 并没有效果，实际的路径在见代码中的 `main` 函数

# llama-factory 大模型训练过程

## 配套库版本问题
 
__为了使用vllm==0.8.3推理，transformers==4.51.0__
__为了进行AWQ量化感知训练，transformers==4.49.0__

记得进行版本切换

## 设置cuda可见性

```bash
export CUDA_VISIBLE_DEVICES=0,1,2
```

# 设置梯子代理

```bash
export http_proxy=http://127.0.0.1:7897
export https_proxy=http://127.0.0.1:7897
```

## 报错与处理

__序列长度不够:__

```
RuntimeError: shape mismatch: value tensor of shape [3, 2193] cannot be broadcast to indexing result of shape [3, 2048]
```

参考 [text](https://github.com/hiyouga/LLaMA-Factory/issues/6844)  

increase cutoff_len to match the sequence length of image tokens


__炸显存:__

炸显存和很多因素有关，其中之一是多轮对话会消耗很多显存，这与上一点报错也有关系

长对话训练的显存占用问题核心在于序列长度与计算资源的平衡，而优化方案需结合算法改进（如稀疏注意力）和工程技巧（如混合精度、模型并行）。实际应用中，通常需要组合多种策略（如DeepSpeed + Megatron-LM + Flash Attention）才能高效训练长序列模型。

以下是DeepSeek给出的具体例子: [结合实例说明长对话与显存占用关系](./结合实例说明长对话与显存占用关系.md)

## 训练有关超参数调节

**超参数相关内容参见：`/home/sy1106/HDD/TZL/LLM-VLM/LLaMA-Factory/src/llamafactory/hparams`文件夹中的文件**

20250723 - QWen3B记录：

```yaml
### model
model_name_or_path: Qwen/Qwen2.5-VL-3B-Instruct
image_max_pixels: 101800
video_max_pixels: 384
trust_remote_code: true

### method
stage: sft
do_train: true
finetuning_type: lora
lora_rank: 8
lora_target: all
# DeepSpeed3 效果比较好
deepspeed: examples/deepspeed/ds_z3_config.json 
# deepspeed: examples/deepspeed/ds_z2_config.json 

### dataset
dataset: r2r_train_424_240_9
template: qwen2_vl
cutoff_len: 2048  # 参考：https://github.com/hiyouga/LLaMA-Factory/issues/6844
# 最多样本数，样本数超过这个数量，会被代码阶段
max_samples: 500000  #334259
overwrite_cache: true
preprocessing_num_workers: 32
dataloader_num_workers: 4
# 保存预处理数据集，能省很多时间
tokenized_path: saves/qwen2_vl-3b/dataset/sft

### output
output_dir: saves/qwen2_vl-3b/lora/sft
logging_steps: 10
save_steps: 10
plot_loss: true
overwrite_output_dir: true
save_only_model: false
report_to: wandb  # choices: [none, wandb, tensorboard, swanlab, mlflow]

### train
# 每个设备上的batch_size
per_device_train_batch_size: 3
# 和batch_size也有关，详细的还要再了解下
gradient_accumulation_steps: 8
learning_rate: 1.0e-4
# 训练轮次
num_train_epochs: 1.0
lr_scheduler_type: cosine
warmup_ratio: 0.1

# rtx titan不支持bf16，所以用fp16
bf16: false
fp16: true

ddp_timeout: 180000000
resume_from_checkpoint: null

# 允许gradient_checkpointing
gradient_checkpointing: true
# 冻结视觉模块
freeze_vision_tower : true

### eval
# val_size: 0.1
# per_device_eval_batch_size: 1
# eval_strategy: steps
# eval_steps: 500

# streaming: true
# accelerator_config:
  # dispatch_batches: false
```


## 数据读取过程

代码位置：/home/sy1106/HDD/TZL/LLM-VLM/LLaMA-Factory/src/llamafactory/data/loader.py
处理逻辑：

```python

# 简单的数据分割
def get_dataset_module(dataset: Union["Dataset", "DatasetDict"]) -> "DatasetModule":
    r"""Convert dataset or dataset dict to dataset module."""
    dataset_module: DatasetModule = {}
    if isinstance(dataset, DatasetDict):  # dataset dict
        if "train" in dataset:
            dataset_module["train_dataset"] = dataset["train"]

        if "validation" in dataset:
            dataset_module["eval_dataset"] = dataset["validation"]
        else:
            eval_dataset = {}
            for key in dataset.keys():
                if key.startswith("validation_"):
                    eval_dataset[key[len("validation_") :]] = dataset[key]

            if len(eval_dataset):
                dataset_module["eval_dataset"] = eval_dataset

    else:  # single dataset
        dataset_module["train_dataset"] = dataset

    return dataset_module

def get_dataset(
    template: "Template",
    model_args: "ModelArguments",
    data_args: "DataArguments",
    training_args: "Seq2SeqTrainingArguments",
    stage: Literal["pt", "sft", "rm", "ppo", "kto"],
    tokenizer: "PreTrainedTokenizer",
    processor: Optional["ProcessorMixin"] = None,
) -> "DatasetModule":
    r"""Get the train dataset and optionally gets the evaluation dataset."""
    # Load tokenized dataset if path exists

    # !如果有预先处理完的数据
    if data_args.tokenized_path is not None:
        if has_tokenized_data(data_args.tokenized_path):
            logger.warning_rank0("Loading dataset from disk will ignore other data arguments.")
            tokenized_data = load_from_disk(data_args.tokenized_path)
            dataset_module = get_dataset_module(tokenized_data)
            if data_args.streaming:
                dataset_module["train_dataset"] = dataset_module["train_dataset"].to_iterable_dataset()

            logger.info_rank0(f"Loaded tokenized dataset from {data_args.tokenized_path}.")
            return dataset_module

        if data_args.streaming:
            raise ValueError("Turn off `streaming` when saving dataset to disk.")

    # 读取数据
    # Load and preprocess dataset
    with training_args.main_process_first(desc="load dataset", local=(not data_args.data_shared_file_system)):
        dataset = _get_merged_dataset(data_args.dataset, model_args, data_args, training_args, stage)
        eval_dataset = _get_merged_dataset(
            data_args.eval_dataset,
            model_args,
            data_args,
            training_args,
            stage,
            return_dict=data_args.eval_on_each_dataset,
        )

    # 预处理数据
    with training_args.main_process_first(desc="pre-process dataset", local=(not data_args.data_shared_file_system)):
        dataset = _get_preprocessed_dataset(
            dataset, data_args, training_args, stage, template, tokenizer, processor, is_eval=False
        )
        if isinstance(eval_dataset, dict):
            for eval_name, eval_data in eval_dataset.items():
                eval_dataset[eval_name] = _get_preprocessed_dataset(
                    eval_data, data_args, training_args, stage, template, tokenizer, processor, is_eval=True
                )
        else:
            eval_dataset = _get_preprocessed_dataset(
                eval_dataset, data_args, training_args, stage, template, tokenizer, processor, is_eval=True
            )

        dataset_dict = split_dataset(dataset, eval_dataset, data_args, seed=training_args.seed)
        
        # 把预处理完的数据保存到本地
        if data_args.tokenized_path is not None:  # save tokenized dataset to disk
            if training_args.should_save:
                dataset_dict.save_to_disk(data_args.tokenized_path)
                logger.info_rank0(f"Tokenized dataset is saved at {data_args.tokenized_path}.")
                logger.info_rank0(f"Please launch the training with `tokenized_path: {data_args.tokenized_path}`.")

        return get_dataset_module(dataset_dict)

```


## 数据随机顺序：

![alt text](images/image.png)

## 训练结果记录

# llama-factory 大模型推理

使用llama-factory 调用 vllm 推理，报错打开文件过多


## 数据预处理过程

数据预处理过程包含创建预处理模板与构建数据集两个主要部分，。

### 创建预处理模板

```python
@dataclass
class Qwen2VLPlugin(BasePlugin):
    @override
    def _preprocess_image(self, image: "ImageObject", **kwargs) -> "ImageObject":
        image = super()._preprocess_image(image, **kwargs)
        if min(image.width, image.height) < 28:
            width, height = max(image.width, 28), max(image.height, 28)
            image = image.resize((width, height))

        if image.width / image.height > 200:
            width, height = image.height * 180, image.height
            image = image.resize((width, height))

        if image.height / image.width > 200:
            width, height = image.width, image.width * 180
            image = image.resize((width, height))

        return image

    @override
    def _regularize_videos(
        self, videos: list["VideoInput"], **kwargs
    ) -> dict[str, Union[list[list["ImageObject"]], list[float]]]:
        results, fps_per_video = [], []
        for video in videos:
            container = av.open(video, "r")
            video_stream = next(stream for stream in container.streams if stream.type == "video")
            sample_indices = self._get_video_sample_indices(video_stream, **kwargs)
            frames: list[ImageObject] = []
            container.seek(0)
            for frame_idx, frame in enumerate(container.decode(video_stream)):
                if frame_idx in sample_indices:
                    frames.append(frame.to_image())

            if len(frames) % 2 != 0:  # qwen2-vl requires even number of frames
                frames.append(frames[-1])

            frames = self._regularize_images(frames, **kwargs)["images"]
            results.append(frames)
            if video_stream.duration is None:
                fps_per_video.append(2.0)
            else:
                fps_per_video.append(len(sample_indices) / float(video_stream.duration * video_stream.time_base))

        return {"videos": results, "fps_per_video": fps_per_video}

    @override
    def _get_mm_inputs(
        self,
        images: list["ImageInput"],
        videos: list["VideoInput"],
        audios: list["AudioInput"],
        processor: "MMProcessor",
    ) -> dict[str, "torch.Tensor"]:
        image_processor: BaseImageProcessor = getattr(processor, "image_processor", None)
        mm_inputs = {}
        if len(images) != 0:
            images = self._regularize_images(
                images,
                image_max_pixels=getattr(processor, "image_max_pixels", 768 * 768),
                image_min_pixels=getattr(processor, "image_min_pixels", 32 * 32),
            )["images"]
            mm_inputs.update(image_processor(images, return_tensors="pt"))

        if len(videos) != 0:
            video_data = self._regularize_videos(
                videos,
                image_max_pixels=getattr(processor, "video_max_pixels", 256 * 256),
                image_min_pixels=getattr(processor, "video_min_pixels", 16 * 16),
                video_fps=getattr(processor, "video_fps", 2.0),
                video_maxlen=getattr(processor, "video_maxlen", 128),
            )
            mm_inputs.update(image_processor(images=None, videos=video_data["videos"], return_tensors="pt"))
            temporal_patch_size: int = getattr(image_processor, "temporal_patch_size", 2)
            if "second_per_grid_ts" in processor.model_input_names:
                mm_inputs["second_per_grid_ts"] = [temporal_patch_size / fps for fps in video_data["fps_per_video"]]

        return mm_inputs

    @override
    def process_messages(
        self,
        messages: list[dict[str, str]],
        images: list["ImageInput"],
        videos: list["VideoInput"],
        audios: list["AudioInput"],
        processor: Optional["MMProcessor"],
    ) -> list[dict[str, str]]:
        self._validate_input(processor, images, videos, audios)
        num_image_tokens, num_video_tokens = 0, 0
        messages = deepcopy(messages)
        image_processor: BaseImageProcessor = getattr(processor, "image_processor")

        merge_length: int = getattr(image_processor, "merge_size") ** 2
        if self.expand_mm_tokens:
            mm_inputs = self._get_mm_inputs(images, videos, audios, processor)
            image_grid_thw = mm_inputs.get("image_grid_thw", [])
            video_grid_thw = mm_inputs.get("video_grid_thw", [])
        else:
            image_grid_thw = [None] * len(images)
            video_grid_thw = [None] * len(videos)

        for message in messages:
            content = message["content"]
            while IMAGE_PLACEHOLDER in content:
                if num_image_tokens >= len(image_grid_thw):
                    raise ValueError(f"`len(images)` is less than the number of {IMAGE_PLACEHOLDER} tokens.")

                image_seqlen = image_grid_thw[num_image_tokens].prod() // merge_length if self.expand_mm_tokens else 1
                content = content.replace(
                    IMAGE_PLACEHOLDER, f"<|vision_start|>{self.image_token * image_seqlen}<|vision_end|>", 1
                )
                num_image_tokens += 1

            while VIDEO_PLACEHOLDER in content:
                if num_video_tokens >= len(video_grid_thw):
                    raise ValueError(f"`len(videos)` is less than the number of {VIDEO_PLACEHOLDER} tokens.")

                video_seqlen = video_grid_thw[num_video_tokens].prod() // merge_length if self.expand_mm_tokens else 1
                content = content.replace(
                    VIDEO_PLACEHOLDER, f"<|vision_start|>{self.video_token * video_seqlen}<|vision_end|>", 1
                )
                num_video_tokens += 1

            message["content"] = content

        if len(images) != num_image_tokens:
            raise ValueError(f"The number of images does not match the number of {IMAGE_PLACEHOLDER} tokens.")

        if len(videos) != num_video_tokens:
            raise ValueError(f"The number of videos does not match the number of {VIDEO_PLACEHOLDER} tokens.")

        return messages

```

QWEN VL的模板目前配置为：

```python
# copied from qwen template
register_template(
    name="qwen2_vl",
    format_user=StringFormatter(slots=["<|im_start|>user\n{{content}}<|im_end|>\n<|im_start|>assistant\n"]),
    format_assistant=StringFormatter(slots=["{{content}}<|im_end|>\n"]),
    format_system=StringFormatter(slots=["<|im_start|>system\n{{content}}<|im_end|>\n"]),
    format_function=FunctionFormatter(slots=["{{content}}<|im_end|>\n"], tool_format="qwen"),
    format_observation=StringFormatter(
        slots=["<|im_start|>user\n<tool_response>\n{{content}}\n</tool_response><|im_end|>\n<|im_start|>assistant\n"]
    ),
    format_tools=ToolFormatter(tool_format="qwen"),
    default_system="You are a helpful assistant.",
    stop_words=["<|im_end|>"],
    mm_plugin=get_mm_plugin(name="qwen2_vl", image_token="<|image_pad|>", video_token="<|video_pad|>"),
)


```

停止token配置：

```python
stop_word = ['<|im_end|>']
self.replace_eos = False
tokenizer.eos_token_id = 151645
tokenizer.pad_token_id = 151643

```


核心数据处理代码： `/LLaMA-Factory/src/llamafactory/data/loader.py` 文件：`_get_preprocessed_dataset` 函数 ：

```
dataset = dataset.map(
    dataset_processor.preprocess_dataset,
    batched=True,
    batch_size=data_args.preprocessing_batch_size,
    remove_columns=column_names,
    **kwargs,
)
```

功能解读参考：[Dataset.map 解读](https://blog.csdn.net/weixin_44815943/article/details/134571497)

要注意，如果这里开多线程处理，就看不到 `preprocess_dataset` 内部的打印

### 图像数据处理：

- 参考 https://docs.vllm.ai/en/latest/api/vllm/multimodal/inputs.html#vllm.multimodal.inputs.HfImageItem
- 输入格式支持 PIL.Image, ndarray, torch.Tensor.


# 从本地安装habitat-0.1.7


直接从conda镜像网站下载 habitat-0.1.7软件包到本地，然后执行安装命令
镜像网站：
1. https://conda.anaconda.org/aihabitat/linux-64
2. https://anaconda.org/aihabitat/habitat-sim


```
conda install --use-local ./xxxxxxx
```

# tb-nightly 问题

清华源无 tb-nightly，需要换到阿里源

```
python -m pip install tb-nightly -i https://mirrors.aliyun.com/pypi/simple
```

```
pip install torch==1.7.1+cu101 torchvision==0.8.2+cu101 torchaudio==0.7.2 -f https://download.pytorch.org/whl/torch_stable.html
```
# 算法整体流程解析

![alt text](structure.drawio.svg)

# ETP——NAV 数据结构解析

核心代码文件：

- `ETPNav/vlnce_baselines/ss_trainer_ETP.py`
- `ETPNav/vlnce_baselines/models/Policy_ViewSelection_ETP.py`

## ETP——NAV 观测数据

`ss_trainer_ETP.py` 文件，`rollout(...)` 核心函数中，`observations` 变量(是一个list，内部包含str和ndarray)是每次从环境中抓取的观测数据。

数据组织如下：`[rgb,depth,rgb30->rgb330,depth30->depth330,instruction_tokens]`

经过 `extract_instruction_tokens，batch_obs,apply_obs_transforms_batch` 三个函数处理后，形成变量`batch`，交给后续网络推理使用


# VLN-CE调试记录

## VLN-CE config文件解析

需要注意的两个对象是最终的 `config`对象和`config.TASK_CONFIG`对象。

在`run.py`文件中，调用 `config = get_config(exp_config, opts)` 得到了最终的配置内容，但是这一函数进行了多层嵌套，具体情况如下：

1. `run.py`文件中调用的 `get_config`函数在`vlnce_baselines/default.py`文件中，在该函数中，首先以 `hibaitat-lab` 项目中`habitat_baselines.config.default._C` 对象中的内容为基础，增加`vlnce_baselines/default.py`文件中`_C`对象中的内容，然后将运行`run.py`文件时传入的配置文件内容（`exp_config`）以及命令行输入的参数（`opts`）传入 `_C` 对象中，最终返回 `_C` 对象。

2. `hibaitat-lab` 项目中`habitat_baselines.config.default.py`文件中，引入了`habitat.config.default.py`文件中的`get_config`函数,该函数能够将`habitat.config.default.py`文件中的`_C`对象与`config.BASE_TASK_CONFIG_PATH`路径配置文件中的内容，合并到 `config.TASK_CONFIG` 对象中。该文件在 `vlnce_baselines/default.py` 文件又进行了一部分操作和修改。

当某一配置内容不确定的时候，可以递归进行查找

针对 `config`对象中的内容，主要有以下几个来源：

1. `run.py`文件运行时，命令行输入的配置文件
2. `vlnce_baselines/default.py`文件中的配置
3. `habitat_baselines.config.default.py`文件中的配置

针对 `config.TASK_CONFIG`对象中的内容，主要有以下几个来源：
1. `run.py`文件运行时，命令行输入的配置文件中 `BASE_TASK_CONFIG_PATH` 路径对应的配置文件
2. `habitat.config.default.py`文件中的配置


## VLN-CE 数据读取与环境初始化过程解析：

综合上一章节的配置文件，我们对VLN-CE数据读取与环境初始化过程进行分析。首先来说数据存储格式

### 数据存储格式

数据包含两个部分: (1)materport场景, (2) VLN 训练与测试数据

- materport场景数据存储在`data/scene_datasets`目录下
- VLN 训练与测试数据存储在`data/datasets`目录下，目前有 R2R 和 RxR 两个数据集。

#### 数据集类注册

在 `VLN-CE/habitat_extensions/task.py` 文件中，使用 `habitat.core.registry` 中的 `@registry.register_dataset()` 方法实现了数据集类的注册，本例中，注册的名字为 `"VLN-CE-v1"` 这与配置对象 `config.TASK_CONFIG.DATASET.TYPE` 对应。

#### 数据集读取

在`VLN-CE/vlnce_baselines/common/env_utils.py` 文件 `construct_envs()` 方法中，调用以下代码实现了数据集的构建（`construct_envs()`方法在 `VLN-CE/vlnce_baselines/dagger_trainer.py` 文件中被调用，trainer以和dataset同样的方法被注册，并在最外层 `run.py`文件中被调用。）:

```python
dataset = make_dataset(config.TASK_CONFIG.DATASET.TYPE)
```
该方法最终创建了 `VLN-CE/habitat_extensions/task.py` 中 `class VLNCEDatasetV1(Dataset)` 类的实例对象。


##### 词嵌入获取

词嵌入内容位于 `VLN-CE/data/datasets/R2R_VLNCE_v1-3_preprocessed/train/train.json.gz` 文件中，文件内容为一个json格式的字符串，解析词嵌入的代码如下，相关内容位于`VLN-CE/habitat_extensions/task.py`文件中。

```python
import gzip
import json

dataset_filename = "data/datasets/R2R_VLNCE_v1-3_preprocessed/train/train.json.gz"
with gzip.open(dataset_filename, "rt") as f:
    deserialized = json.loads(f.read())
    word_list=deserialized["instruction_vocab"]["word_list"]
    word2idx_dict = {w: n_w for n_w, w in enumerate(word_list)}
```

##### 运动数据与参考轨迹数据获取

```python
class gt_follower:
    """
    A class to represent a follower in ground truth trajectory following.
    """
    def __init__(self):
        pass
    
    def dataset_parse(self):
        dataset_filename = "data/datasets/R2R_VLNCE_v1-3_preprocessed/train/train.json.gz"
        dataset_gt_filename = "data/datasets/R2R_VLNCE_v1-3_preprocessed/train/train_gt.json.gz"
        
        with gzip.open(dataset_gt_filename, "rt") as f:
            deserialized = json.loads(f.read())
            count = 0
            for i in range(len(deserialized['1']['actions'])):
                if(deserialized['1']['actions'][i] == 1):
                    print(deserialized['1']['actions'][i],deserialized['1']['locations'][count])
                    count += 1
                else :
                    print(deserialized['1']['actions'][i])
```

运动数据中`0,1,2,3`对应 `[stop, move forward 0.25m, turn-left or turn-right 15 degrees]`
`location`的三维数据暂时不知道啥意思，怀疑中间的维度是z，因为没有变化过，但是 左边和右边是怎么规定的，不太清楚了，可能得看看 `habitat-lab` 库里面的规定

根据代码的注释，可能是以下意思

```python
"""
episode_id: id of episode in the dataset
scene_id: id of scene inside the simulator.
start_position: numpy ndarray containing 3 entries for (x, y, z).
start_rotation: numpy ndarray with 4 entries for (x, y, z, w)
    elements of unit quaternion (versor) representing agent 3D
    orientation.
goals: list of goals specifications
reference_path: List of (x, y, z) positions which gives the reference
    path to the goal that aligns with the instruction.
instruction: single natural language instruction guide to goal.
trajectory_id: id of ground truth trajectory path.
"""

```

### 仿真任务解析

#### 仿真运动

仿真运动的可选项与幅度配置参考`VLN-CE/habitat_extensions/config/vlnce_task.yaml` 配置文件中：

```yaml
SIMULATOR:
  FORWARD_STEP_SIZE: 0.25
  TURN_ANGLE: 15
TASK:
  POSSIBLE_ACTIONS: [STOP, MOVE_FORWARD, TURN_LEFT, TURN_RIGHT]
```

对应的代码部分位于：

`latest_version/habitat-lab/habitat-lab/habitat/sims/habitat_simulator/habitat_simulator.py`
```python
        agent_config.action_space = {
            0: habitat_sim.ActionSpec("stop"),
            1: habitat_sim.ActionSpec(
                "move_forward",
                habitat_sim.ActuationSpec(
                    amount=self.habitat_config.forward_step_size
                ),
            ),
            2: habitat_sim.ActionSpec(
                "turn_left",
                habitat_sim.ActuationSpec(
                    amount=self.habitat_config.turn_angle
                ),
            ),
            3: habitat_sim.ActionSpec(
                "turn_right",
                habitat_sim.ActuationSpec(
                    amount=self.habitat_config.turn_angle
                ),
            ),
        }
```

## VLN训练数据生成全记录

### 生成并保存图像数据及运动数据

代码文件保存位置：`/home/sy1106/HDD/TZL/VLN/latest_version/my_habitat-sample/gt_follower_example.py`

核心内容位于 `reference_path_example()` 函数中。

首先获取配置文件，配置文件参考 habitat-lab项目给出的 r2r数据集配置文件构建，目前的配置中采用了 envdrop 分割，该分割来源于[R2R-EnvDrop](https://github.com/airsplay/R2R-EnvDrop) 项目，将原有的R2R数据集划分为14万条轨迹数据。配置文件中关闭了顶视图传感器，经过测试该传感器十分消耗计算时间。

随后，使用配置文件创建了 `SimpleRLEnv` 环境， 这一环境调用了hibatit中的代码，会自动完成场景渲染等工作。

在之后是创建核心的 `follower` ，这一对象会执行动作指令（本来是获取环境数据后，生成下一步动作，这里我们直接跟踪数据集中的最优动作）

接下来就是执行动作-认知过程，首先我们左转再右转，然后获取当前感知数据，这样来解决原本的程序中，无法获取第一帧运动之间图像的问题，记录第一帧观测量后，开始执行动作，然后我们将这个过程中的 感知图像与最优动作进行记录。

记录的方式为：__创建与eposide名字相同的文件夹，然后将图片压缩为jpg格式写入，动作序列写入到一个txt文档中__

### 生成 vln 训练所需要的 JSON 文件

代码文件保存位置：`/home/sy1106/HDD/TZL/VLN/latest_version/my_habitat-sample/utils/llama-fac-json-create.py`

都是比较细碎的工作，根据上面生成的运动数据和rgb感知数据，生成QWEN训练需要的json文件。


## RXR数据读取与解析

![alt text](images/image-1.png)

/home/sy1106/HDD/TZL/VLN/latest_version/habitat-lab/habitat-lab/habitat/config/default_structured_configs.py 文件中定义了 hydra库中配置文件读取的默认配置文件，将配置文件与代码数据结构体进行了对应。读取RXR数据集，需要编写对应的位置文件和配置文件加载方式。 todo

# mast3r 调试记录

代码中使用了 `from functools import cache` 这一特性需要安装python 3.9以上版本，否则会报错。

## 使用 mast3r 提取两帧图像的相对运动关系

[mast3r_demo_without_gradio.py](../tzl_mast3r_demo/demo_without_gradio.py)

代码调用 mast3r 项目中的 `get_reconstructed_scene()` 方法，实现两帧图像之间空间变换的推理，并生成了三维模型。（代码560行作业为原始生成三维模型的代码，原始的三维模型中还包含了利用两相机重建得到的空间点云）

在此基础上基于首张图像的坐标系（z向图像前，x向右，y向下）y轴反方向进行投影，得到 xoz 平面上图像之间的位置关系。（这一步添加了一个假设，即：所有图像拍摄时，相机坐标系的y轴朝向与重力方向相同）。

随后生成俯视视角的图像位置关系与两机位之间的动作序列。动作序列可用于语视觉大模型训练。


# 基于信息熵的视频数据集挑选

## Navila

`navila`项目中，作者在构建基于现实视频数据集的过程中，采用了基于熵的采样，保证丰富性和代表性，原文表达为：`We process them into 20K trajectories using entropy-based sampling (Lin et al., 2023) to ensure variety and representativeness.` 其中，参考文献为：`Learning Vision-and-Language Navigation from YouTube Videos`

![alt text](images/image.png)

## Learning Vision-and-Language Navigation from YouTube Videos 

`Learning Vision-and-Language Navigation from YouTube Videos` 文章中作者希望选择信息量最大的图像作为节点特征，作者认为分类熵最小的图像包含了对特定类更多的信息，因此选择了最细分类熵的图像代表当前运动节点。原文表达为：`Inspired by EATA [45], an image with lower classification entropy is more reliable, containing more information relevant to a specific class (room type in our case). We thus propose to select an image with the lowest classification entropy to represent the current view of a node.`

### 源码阅读

`YouTube-VLN/scripts/video_process/generate_CLIP_captions.py` 文件中定义了香农信息熵：

```python
def ShannonEntropy(similarity):
    shannonEnt = 0.0
    for i in range(len(similarity[0])):
        p = similarity[0,i] + 1e-7
        shannonEnt -= p * log(p, 2)

    return float(shannonEnt.cpu())

# 路径设置与目录创建
# 输出目录
captioned_images_root = args.outfile
#输入视频帧路径
raw_images_root = args.video_path
for vid in tqdm(videos):
    img_path = os.path.join(video_path, vid)
    imgs = os.listdir(img_path)

    if not os.path.exists(captioned_images_root + f"/{vid}/"):
        os.makedirs(captioned_images_root + f"/{vid}/")
    caption_infos = {}

    # 单帧图像处理
    for img in imgs:
        if img.split(".")[-1] != "jpg":
            continue
        # 特征提取与L2归一化，从预先生成的 features 字典中加载当前帧的特征向量 cand_feat。
        long_id = vid + '_' + img
        cand_feat = torch.from_numpy(features[long_id]).to(device).half()
        cand_feat /= cand_feat.norm(dim=-1, keepdim=True)
        area_text_features /= area_text_features.norm(dim=-1, keepdim=True)
        object_text_features /= object_text_features.norm(dim=-1, keepdim=True)

        # 相似度计算与类别预测
        area_similarity = (100.0 * cand_feat @ area_text_features.T).softmax(dim=-1)
        object_similarity = (100.0 * cand_feat @ object_text_features.T).softmax(dim=-1)
        area_pred = areas[area_similarity.topk(TOP_K)[1]]
        object_pred = objects[object_similarity.topk(TOP_K)[1]]

        # 熵计算与结果保存
        SE = ShannonEntropy(area_similarity)
        caption_infos[img] = [area_pred + ' with ' + object_pred, \
                              {"ShannonEntropy": SE},\
                              {"similarity": float(area_similarity.topk(TOP_K)[0].cpu())}]

    # 排序与保存结果
    sorted_keys = sorted(caption_infos.keys())
    caption_infos_after = { e: caption_infos[e] for e in sorted_keys}    
    with open(captioned_images_root + f"/{vid}/captions_SE.json", 'w') as f:
        json.dump(caption_infos_after, f, indent=4)
```


对于函数 `ShannonEntropy(similarity)` 输入 `similarity` 是一个形状为 (1, C) 的张量，表示模型对当前帧属于 C 个类别的概率分布（通过Softmax归一化），函数本身用于计算概率分布的香农熵，熵值越高，代表模型预测越不确定，**对应的场景更复杂**。函数中 `1e-7` 用于避免 `p=0` 导致数值计算错误。使用二进制对数`（log(p, 2)）`，熵单位为比特（bit）。

4. 关键改进建议
   
- 熵计算范围：当前仅计算区域相似度的熵，可增加物体相似度的熵值综合判断。
- 动态阈值：根据视频整体熵分布自适应设定阈值，而非固定值。
- 批量处理优化：使用矩阵运算加速熵计算，避免逐帧循环。


`YouTube-VLN/scripts/inverse_action/main.py` 源码文件中实现了基于香农熵（Shannon Entropy）从长视频中提取关键帧。源码核心逻辑为：

1. 检测场景类型切换（如从“客厅”切换到“厨房”）。
2. 在每个连续的同场景类型片段中，选择熵值最小的帧作为关键帧（即模型预测最确定的帧）。

```python
if args.use_se_key_frames:
            # leverage shannon entropy to decide the key frames
            last_room_type = None
            last_room_SE = []
            last_room_SE_index = []

            with open(f'{args.location}/{args.caption_path}/{vid}/captions_SE.json', "r") as fid:
                json_reader = json.load(fid)
                valid_frames = []
        
                for x in range(1,len(all_frame)+1):
                    if front!= 0 and x <= front:
                        continue
                    if back!= 0 and x >= back:
                        continue
                    
                    # 判断是否为有效帧
                    if valid_frame(x) and valid_frame_new(x):
                        valid_frames.append(f'%04d.jpg' % (x))
                        # 检查是否为有效帧（需自定义 valid_frame 和 valid_frame_new 函数）
                        if json_reader[f'%04d.jpg' % (x)][0].split(' with ')[0] != last_room_type:

                            # After seeing a new room type, look for the frame with the least information entropy in the previous room type
                            # 场景切换时，从上一场景中选择熵最小的帧作为关键帧
                            if last_room_type != None:        
                                key_frames.append(last_room_SE_index[last_room_SE.index(min(last_room_SE))])
                            # clear
                            last_room_SE = []
                            last_room_SE_index = []
                            # Add information about seeing this room type for the first time
                            last_room_SE.append(json_reader[f'%04d.jpg' % (x)][1]['ShannonEntropy'])
                            last_room_SE_index.append(x)
                            # 重置为新场景类型
                            last_room_type = json_reader[f'%04d.jpg' % (x)][0].split(' with ')[0]

                        else:
                            # 同一场景类型下，持续记录熵值和帧序号
                            last_room_SE.append(json_reader[f'%04d.jpg' % (x)][1]['ShannonEntropy'])
                            last_room_SE_index.append(x)
                    # Prevent the situation where there is only one type of group
                    # 处理最后一组场景（避免漏选）
                    if x == len(listdir(f'{args.location}/raw_frames/{vid}')) and \
                        last_room_type == json_reader[f'%04d.jpg' % (x)][0].split(' with ')[0]:
                        key_frames.append(last_room_SE_index[last_room_SE.index(min(last_room_SE))])
```


#### 关键设计分析

- [x] test

__为何选择熵最小的帧？__

高熵：表示模型预测不确定性高（可能是复杂场景或分布偏移）。

低熵：表示模型预测确定性高（可能是场景中的典型帧）。

设计假设：在同一场景类型中，熵最小的帧最能代表该场景的“标准”状态（如厨房场景中最清晰的厨房画面）。

__场景切换检测的局限性__

依赖预测标签的准确性（如 area_pred 是否可靠）。
若场景类型频繁抖动（如预测标签在“客厅”和“走廊”间反复切换），可能导致关键帧冗余。

#### 优化建议
动态阈值选择： 若同一场景类型的熵值波动较大，可结合局部窗口（如相邻10帧）选择熵值最低的帧。

多指标融合： 综合熵值、相似度得分（json_reader[...][2]["similarity"]）选择关键帧。

后处理去重： 对 key_frames 中的帧进行相似性检测（如特征距离），避免重复选择相似关键帧。

#### 总结
这段代码通过检测场景类型切换，并在每个场景片段中选择熵值最小的帧作为关键帧，实现了基于模型预测确定性的关键帧提取。其核心思想是利用熵值量化帧的代表性，适用于视频摘要、场景分析等任务。实际应用中需结合场景类型预测的准确性和业务需求调整策略。

## 视频处理---Nvidia VideoProcessing 库安装

根据官方的教程直接安装即可，但是会遇到一个问题。

```shell
pip insatll .
```

之后报错，

原因是：`VideoProcessingFramework/src/PyNvCodec/CMakeLists.txt` 文件中，`FetchContent` 部分安装的 pybind-2.10.0 版本的CMAKE文件中，有 `cmake_minimum_required(VERSION 3.4)` 的版本限制，后续的cmake版本要这里必须在3.5版本以上，所以会报错，这里我们修改`FetchContent`部分代码如下，下载更新版本的`pybind`即可

```cmake
include(FetchContent)
fetchcontent_declare(
    pybind11
    # URL https://github.com/pybind/pybind11/archive/v2.10.0.tar.gz
    URL https://github.com/pybind/pybind11/archive/v2.13.6.tar.gz
    # URL_HASH
    #     SHA256=eacf582fa8f696227988d08cfc46121770823839fe9e301a20fbce67e7cd70ec
)
fetchcontent_makeavailable(pybind11)
```

## 视频处理---Nvidia VideoProcessing 库使用

参考自带的sample文件夹中代码即可，这里我们主要参照了`SampleTorchSegmentation.py` 文件


# LORA 介绍与代码组织

[text](https://lightning.ai/lightning-ai/studios/code-lora-from-scratch?section=featured)